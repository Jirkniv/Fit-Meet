import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    imageContainer: {
        position: 'absolute',
        top: 24,
        left: 20,
        width: 16,
        height: 20,
        zIndex: 10,
      },
image: {
            width: '100%',
            height: '100%',
            resizeMode: 'contain',
          },
});